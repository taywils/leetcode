#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

class Solution {
public:
	string convert(string s, int nRows) {
		string answer = "";
		if(0 == s.length() || nRows < 1) {
			return "";
		}
		return answer;
	}
};

int main() {
	auto s = new Solution();
	return 0;
}
